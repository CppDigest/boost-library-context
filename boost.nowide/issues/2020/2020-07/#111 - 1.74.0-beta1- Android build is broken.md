# #111 - 1.74.0-beta1: Android build is broken [Closed]

> Username: gjasny  
> Created at: 2020-07-21 15:24:20 UTC  
> Updated at: 2020-07-22 14:05:12 UTC  
> Closed at: 2020-07-22 14:05:12 UTC  
> Url: https://github.com/boostorg/nowide/issues/111  

When building with NDK r20 for `armeabi-v7a` with `android-19` backward compatibility I get the following error:  
  
```  
libs/nowide/src/filebuf.cpp:62:30: error: use of undeclared identifier 'ftello'; did you mean 'ftell'?  
            const auto pos = BOOST_NOWIDE_FTELL(file);  
                             ^~~~~~~~~~~~~~~~~~  
                             ftell  
libs/nowide/src/filebuf.cpp:29:28: note: expanded from macro 'BOOST_NOWIDE_FTELL'  
#define BOOST_NOWIDE_FTELL ftello  
                           ^  
libs/nowide/src/filebuf.cpp:60:24: note: 'ftell' declared here  
        std::streampos ftell(FILE* file)  
                       ^  
libs/nowide/src/filebuf.cpp:76:20: error: use of undeclared identifier 'fseeko'; did you mean 'fseek'?  
            return BOOST_NOWIDE_FSEEK(file, static_cast<BOOST_NOWIDE_OFF_T>(offset), origin);  
                   ^~~~~~~~~~~~~~~~~~  
                   fseek  
libs/nowide/src/filebuf.cpp:30:28: note: expanded from macro 'BOOST_NOWIDE_FSEEK'  
#define BOOST_NOWIDE_FSEEK fseeko  
                           ^  
libs/nowide/src/filebuf.cpp:70:13: note: 'fseek' declared here  
        int fseek(FILE* file, std::streamoff offset, int origin)  
            ^  
libs/nowide/src/filebuf.cpp:48:13: error: static_assert failed "Mixed sign comparison can lead to problems below"  
            static_assert(std::is_signed<T>::value == std::is_signed<U>::value,  
            ^             ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~  
libs/nowide/src/filebuf.cpp:57:20: note: in instantiation of function template specialization 'boost::nowide::detail::is_in_range<long long, std::__ndk1::fpos<mbstate_t> >' requested here  
            return is_in_range<T>(value) ? static_cast<T>(value) : T(-1);  
                   ^  
libs/nowide/src/filebuf.cpp:67:20: note: in instantiation of function template specialization 'boost::nowide::detail::cast_if_valid_or_minus_one<long long, std::__ndk1::fpos<mbstate_t> >' requested here  
            return cast_if_valid_or_minus_one<std::streamoff>(pos);  
                   ^  
3 errors generated.  
```  
  
In the Android `stdio.h` the `ftello` and friends are restricted to `android-24` and newer:  
```  
#if __ANDROID_API__ >= 24  
int fgetpos(FILE* __fp, fpos_t* __pos) __RENAME(fgetpos64) __INTRODUCED_IN(24);  
int fsetpos(FILE* __fp, const fpos_t* __pos) __RENAME(fsetpos64) __INTRODUCED_IN(24);  
int fseeko(FILE* __fp, off_t __offset, int __whence) __RENAME(fseeko64) __INTRODUCED_IN(24);  
off_t ftello(FILE* __fp) __RENAME(ftello64) __INTRODUCED_IN(24);  
#endif /* __ANDROID_API__ >= 24 */  
```  
  
Somehow the LFS check return true:  
```  
- Has Large File Support   : yes  
```  
  
Should I prepare instructions how to reproduce?

---

## Comment 1

> Username: Flamefire  
> Created at: 2020-07-21 15:40:31 UTC  
> Url: https://github.com/boostorg/nowide/issues/111#issuecomment-661937498  

Thanks for reporting. I'm having trouble understanding how this can happen as the check specifically checks for the availability of those functions.  
How do you invoke `b2`? Any config options set? Any debug info from b2 or so?

---

## Comment 2

> Username: gjasny  
> Created at: 2020-07-21 16:06:25 UTC  
> Url: https://github.com/boostorg/nowide/issues/111#issuecomment-661952265  

If I make the check define also `_FILE_OFFSET_BITS` it fails like the source code:  
```  
#define _LARGEFILE_SOURCE  
#ifndef _FILE_OFFSET_BITS  
#define _FILE_OFFSET_BITS 64  
#endif  
```  
  
```  
~/Android/android-ndk-r20/toolchains/llvm/prebuilt/linux-x86_64/bin/armv7a-linux-androideabi19-clang++ -c check_lfs_support.cpp   
check_lfs_support.cpp:25:11: error: use of undeclared identifier 'fseeko'; did you mean 'fseek'?  
    (void)fseeko(f, off_t(0), SEEK_CUR);  
          ^~~~~~  
          fseek  
/home/gregorj/Android/android-ndk-r20/toolchains/llvm/prebuilt/linux-x86_64/bin/../sysroot/usr/include/stdio.h:175:5: note: 'fseek' declared here  
int fseek(FILE* __fp, long __offset, int __whence);  
    ^  
check_lfs_support.cpp:26:11: error: use of undeclared identifier 'ftello'; did you mean 'ftell'?  
    (void)ftello(f);  
          ^~~~~~  
          ftell  
/home/gregorj/Android/android-ndk-r20/toolchains/llvm/prebuilt/linux-x86_64/bin/../sysroot/usr/include/stdio.h:176:6: note: 'ftell' declared here  
long ftell(FILE* __fp);  
     ^  
2 errors generated.  
```

---

## Comment 3

> Username: Flamefire  
> Created at: 2020-07-21 16:15:10 UTC  
> Url: https://github.com/boostorg/nowide/issues/111#issuecomment-661957253  

Thanks, that is a good hint. Do you mind attaching or linking the used stdio.h for me to inspect the cause?

---

## Comment 4

> Username: Flamefire  
> Created at: 2020-07-21 17:24:16 UTC  
> Url: https://github.com/boostorg/nowide/issues/111#issuecomment-661996835  

Ok found it:  
```  
#if defined(__USE_FILE_OFFSET64)  
  
#if __ANDROID_API__ >= 24  
int fgetpos(FILE* __fp, fpos_t* __pos) __RENAME(fgetpos64) __INTRODUCED_IN(24);  
int fsetpos(FILE* __fp, const fpos_t* __pos) __RENAME(fsetpos64) __INTRODUCED_IN(24);  
int fseeko(FILE* __fp, off_t __offset, int __whence) __RENAME(fseeko64) __INTRODUCED_IN(24);  
off_t ftello(FILE* __fp) __RENAME(ftello64) __INTRODUCED_IN(24);  
#endif /* __ANDROID_API__ >= 24 */  
  
#else  
int fgetpos(FILE* __fp, fpos_t* __pos);  
int fsetpos(FILE* __fp, const fpos_t* __pos);  
int fseeko(FILE* __fp, off_t __offset, int __whence);  
off_t ftello(FILE* __fp);  
#endif  
```  
  
So it does define `ftello` but NOT when `__USE_FILE_OFFSET64` is defined and the API level is below 24... Weird. Will fix the check, thank you very much for pinpointing it!

---

## Comment 5

> Username: gjasny  
> Created at: 2020-07-21 17:25:57 UTC  
> Url: https://github.com/boostorg/nowide/issues/111#issuecomment-661997824  

I can attach it tomorrow or you download the NDK: https://developer.android.com/ndk/downloads

---

## Comment 6

> Username: Flamefire  
> Created at: 2020-07-21 17:43:45 UTC  
> Url: https://github.com/boostorg/nowide/issues/111#issuecomment-662008074  

I created #112 with a fix. You can try it be applying the PR as a patch (https://github.com/boostorg/nowide/pull/112.patch) onto the 1.74.0-beta1. Let me know if that works!  
  
Note: This will make the API fallback to `ftell`. However this might still support 64 bit files on 64 bit systems as `long` in the linux model is (usually) 64 bits, same as `off_t` in that case.

---

## Comment 7

> Username: gjasny  
> Created at: 2020-07-22 09:35:19 UTC  
> Url: https://github.com/boostorg/nowide/issues/111#issuecomment-662351273  

With your patch applied it now builds fine. Will you ask to include it in the next 1.74.0 beta or rc?  
  
Thanks!  
Gregor

---

## Comment 8

> Username: Flamefire  
> Created at: 2020-07-22 12:47:52 UTC  
> Url: https://github.com/boostorg/nowide/issues/111#issuecomment-662432856  

Thanks for confirming. Yes I'll try to have that included but don't see why it won't
